package com.example;

import java.util.ArrayList;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class CarServerApplication {

	public static void main(String[] args) {
		SpringApplication.run(CarServerApplication.class, args);
		
		
		
		/*
		 * 
		 * New Spring boot JPA + Web
		 * Entity: Student --> id, name, email, grade
		 * Repository + facade.
		 * Controller --> make sure to handle exceptions and return ResponseEntity
		 * Add Swagger2 + Swagger UI
		 * 
		 */

		
	}

}
